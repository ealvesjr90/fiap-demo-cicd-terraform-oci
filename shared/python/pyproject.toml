[project]
name = "solidarytech"
version = "0.1.0"
description = "Shared utilities for SolidaryTech Python microservices"
requires-python = ">=3.9"
dependencies = [
    "Flask>=2.2",
    "python-dotenv>=0.21",
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
