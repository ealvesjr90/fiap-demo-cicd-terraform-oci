apiVersion: v1
kind: Secret
metadata:
  name: newrelic-secret
  namespace: togglemaster
type: Opaque
stringData:
  # IMPORTANT: Provide the real license key via sealed-secrets, external-secrets,
  # or CI/CD injection. Never commit actual keys to version control.
  license-key: CHANGE_ME
